describe('Testando orderRepositores', () => {
  it('Número Igual a 1', () => {
    const number = 1;

    expect(number).toBe(1);
  });
});

describe('Testando...', () => {
  test('Nome igual a Joao', () => {
    const name = 'Joao';

    expect(name).toBe('Joao');
  });
});
